@AGENTS.md

# Stack y entorno
Read README.md at the start of every session. It documents the actual stack (PostgreSQL via Supabase, not SQLite), required env vars (DATABASE_URL + DIRECT_URL), and integration architecture. Never assume defaults that contradict it.

# Commits
Never add "Co-Authored-By: Claude" or any Claude/Anthropic signature to commit messages.
